---
import { cn } from "@/lib/utils";
import { buttonVariants } from "@/components/ui/button";
import { Icon } from "astro-icon/components";
import { siteConfig } from "@/config/site";
---

<section class="space-y-6 py-12 sm:py-20 lg:py-20">
  <div class="container flex max-w-5xl flex-col items-center gap-5 text-center">
    <a
      href="https://tiktok.com/@twerksocial"
      class={cn(
        buttonVariants({ variant: "outline", size: "sm", rounded: "full" }),
        "px-4"
      )}
      target="_blank"
    >
      <span class="mr-3">💃</span> Follow us on{" "}
      <Icon name="ri:tiktok-fill" class="ml-2 size-3.5" />
    </a>

    <h1
      class="text-balance font-heading text-4xl sm:text-5xl md:text-6xl lg:text-[66px]"
    >
      💃 TWERK.DANCE{" "}
      <span class="text-gradient_indigo-purple font-extrabold">SPORTS</span
      > BETTING
    </h1>

    <p
      class="max-w-2xl text-balance leading-normal text-muted-foreground sm:text-xl sm:leading-8"
    >
      😍 Twerking is a livestream blockchain sport powered by fans who prediction
      bet on which Twerkrz will will verus and earn the most Bitcoin Solana $WATAA tips 
    </p>

    <div class="flex justify-center space-x-2 md:space-x-4">
      <a
        href="https://bio.twerk.dance"
        class={cn(buttonVariants({ size: "lg", rounded: "full" }), "gap-2")}
      >
        <span>Social App</span>
        <Icon name="lucide:arrow-right" class="size-4" />
      </a>
      <a
        href="https://bio.twerk.dance/?fluent-booking=calendar&host=hahz"
        class={cn(buttonVariants({ size: "lg", rounded: "full" }), "gap-2")}
      >
        <span>Book A Show</span>
        <Icon name="lucide:arrow-right" class="size-4" />
      </a>
    </div>
  </div>
</section>
