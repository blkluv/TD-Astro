---
import MainLayout from "@/layouts/main-layout.astro";
import { Image } from "astro:assets";
import { hobbies } from "@/config/about.ts";
import { Button } from "@/components/ui/button";
import { Icon } from "astro-icon/components";
---

<MainLayout
  title="About TWERK·DANCE"
  description="The first Web5 social marketplace for movement, music, and manifesting digital abundance."
>
  <section class="space-y-5">
    <!-- 💃 Hero -->
    <section class="relative container max-w-screen-md mb-28 mt-16 sm:mt-20 lg:mt-28">
      <div class="flex h-full flex-col justify-between">
        <div class="grid gap-8 sm:grid-cols-2">
          <div>
            <h1 class="text-4xl font-heading text-foreground">💃 TWERK·DANCE</h1>
            <p class="text-base text-muted-foreground mt-0.5">
              The Web5 Social Marketplace for Twerkers
            </p>
          </div>

          <div>
            <p class="mb-12 text-base text-muted-foreground leading-relaxed">
              Welcome to <strong>TWERK.DANCE</strong> — the world’s first 
              <strong>Web5 creator movement</strong> built for twerkers, dancers, and digital artists
              to <em>earn, connect, and move</em> in rhythm with blockchain energy.  
              <br /><br />
              Every dance becomes data, every vibe becomes value, and every performance 
              is verified by <strong>Phantom 🏦 + What3Words 📍</strong>.
            </p>

            <div class="flex flex-wrap gap-3">
              <Button className="rounded-xl">
                <Icon name="lucide:video" class="mr-2 size-4" />
                <a href="https://www.tiktok.com/@twerksocial" target="_blank">
                  Follow @TwerkSocial
                </a>
              </Button>

              <Button variant="outline" className="rounded-xl">
                <Icon name="lucide:globe" class="mr-2.5 size-4" />
                <a href="https://bio.twerk.dance" target="_blank">
                  Explore TWERK Portals
                </a>
              </Button>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- 🪩 Mosaic Gallery -->
    <div class="relative z-10 flex flex-col justify-center overflow-hidden lg:overflow-visible pb-16">
      <div class="mx-auto flex w-max -translate-x-[15%] justify-center lg:w-full lg:translate-x-0">
        <div class="relative z-10 mx-auto max-w-4xl px-4 md:max-w-6xl md:px-12 lg:max-w-5xl lg:px-6 xl:max-w-[80rem] xl:px-6 2xl:px-0">
          <div class="grid grid-cols-4 items-center gap-4 md:gap-6">

            <!-- Column 1 -->
            <div class="space-y-4 md:space-y-6">
              <Image class="rounded-xl" src={hobbies[0].image} alt={hobbies[0].alt} width={800} height={1226} loading="eager" />
              <div class="h-52 overflow-hidden rounded-xl">
                <Image class="w-full h-full object-cover" src={hobbies[1].image} alt={hobbies[1].alt} width={800} height={1334} />
              </div>
            </div>

            <!-- Column 2 -->
            <div class="space-y-4 md:space-y-6">
              <div class="h-48 overflow-hidden rounded-xl">
                <Image class="w-full h-full object-cover" src={hobbies[2].image} alt={hobbies[2].alt} width={800} height={983} loading="eager" />
              </div>
              <Image class="rounded-xl" src={hobbies[3].image} alt={hobbies[3].alt} width={800} height={1108} loading="eager" />
              <div class="h-48 overflow-hidden rounded-xl">
                <Image class="w-full h-full object-cover" src={hobbies[4].image} alt={hobbies[4].alt} width={1260} height={750} />
              </div>
            </div>

            <!-- Column 3 -->
            <div class="space-y-4 md:space-y-6">
              <div class="h-56 overflow-hidden rounded-xl">
                <Image class="w-full h-full object-cover" src={hobbies[5].image} alt={hobbies[5].alt} width={800} height={1334} loading="eager" />
              </div>
              <div class="h-80 w-full overflow-hidden rounded-xl xl:h-[28rem]">
                <Image class="w-full h-full object-cover" src={hobbies[6].image} alt={hobbies[6].alt} width={801} height={2477} />
              </div>
            </div>

            <!-- Column 4 -->
            <div class="space-y-4 md:space-y-6">
              <div class="overflow-hidden rounded-xl bg-white">
                <Image src={hobbies[7].image} alt={hobbies[7].alt} width={800} height={900} loading="eager" />
              </div>
              <div class="overflow-hidden rounded-xl bg-white">
                <Image src={hobbies[8].image} alt={hobbies[8].alt} width={800} height={900} />
              </div>
            </div>

          </div>
        </div>
      </div>
    </div>
  </section>
</MainLayout>
